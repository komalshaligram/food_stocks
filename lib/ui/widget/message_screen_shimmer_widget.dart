import 'package:flutter/material.dart';

class MessageScreenShimmerWidget extends StatelessWidget {
  const MessageScreenShimmerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
